# UEFI OS

Can be run in a docker container or natively.

## Docker

Requirements:
- `docker`

```bash
$ ./build_run_docker.sh
```

## Native

Requirements:
- `qemu-system` 
- `mtools` 
- `ovmf` 
- `build-essential` 
- `parted` 

Do `make`
